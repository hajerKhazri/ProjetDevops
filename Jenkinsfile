pipeline {
    agent any
    stages {
        stage('Unblock Jenkins') {
            steps {
                sh 'echo "🚀 JENKINS DÉBLOQUÉ - 5.3GB LIBRES!"'
                sh 'mvn --version'
                sh 'docker --version'
            }
        }
    }
}